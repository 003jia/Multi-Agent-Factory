# 熔炼镇 / 多 Agent 工厂 — 工程基线与路线图

> 更新日期：2026-07-01（含当日二次审查 + P0 修复）· 本文件是**唯一权威的工程现状 + 计划事实源**。
> 配套：产品 / 视觉基线见 [当前开发基线-熔炼镇.md](当前开发基线-熔炼镇.md)（保留，不在本文重复）。
> 本文取代并合并了原先散落的 6 份文档（MVP 实施计划、校验清单、重规划、Gas Town 分工设计、api-ai 计划与优化记录）。

---

## 一、当前真实状态（已实测，含真实跑通验证）

`typecheck` exit 0 · `build` ok · `build:electron` ok · **`npm test` 41/41 全绿（9 个测试文件）**。核心代码 + 测试 + 文档抽样约 8572+ 行。

它现在实际是：一个 **Electron 桌面工作台**（Web 同源可跑），完成 7 步炼制流程——

> 扫描真实项目 → 选文件/给约束 → 生成需求 → 确认 → 生成计划 → 确认 → 拆 work item + 语义分派 → 生成补丁集(PatchSet) → 沙箱跑真实 `tsc` 校验 → 审查门禁 → 应用补丁到**真实磁盘** → 提交/应用记录；问题可回流到责任 Agent 修复重跑。

AI 支持 **Anthropic 与 OpenAI 兼容格式**（可配 baseUrl，端到端含设置页 UI + 本地持久化）；无 Key 时降级 **Mock**（离线、测试确定）。

### 本轮真实跑通验证（干净环境 + 生产构建单进程，非截图猜测）

用脚本 + 浏览器预览走了一遍完整生命周期（创建→需求确认→计划确认→生成→审查→提交），结论：

- ✅ **状态机、持久化、HTTP 全链路是真的**：7 步全部真实推进，SQLite 落库，语义分派真的按能力打分把 5 个 work item 分给了 3 个不同 agent（后端/审查/前端），不是随机或摆设。
- ✅ **Mock 模式生成内容现在有醒目占位提示**（P0.2 已修）：产物面板顶部有黄色"当前为 Mock 演示内容"横幅，每个文件也标"占位模板，非真实生成内容"，浏览器截图 + 无障碍树已验证渲染正确。
- ✅ **"跳过沙箱校验"不再显示为"通过"**（P0.3 已修）：未绑定项目时 finding 的 `status` 从 `resolved` 改为 `open`（severity 仍是 `warning`，不阻断流程），文案明确写"未运行真实静态校验（不是"已通过"）"；对应 PatchSet 的 `applyStatus` 从误导性的 `verified` 改为诚实的 `pending`。已用新建任务实测确认。

---

## 二、本轮 P0 修复记录（2026-07-01，全部已验证）

1. **`tests/api.test.ts` 回归** — 已被并行开发修复（新增 `allowHttpProjectScan` 选项 + 专门测试 403 场景），本轮复核确认 41/41 全绿，无需重复处理。
2. **Mock 生成内容缺少占位提示** — 已修：[src/App.tsx](../src/App.tsx) `ResultStageCard`/`ArtifactSummary` 新增 `aiStatus` 传参，`!aiStatus.aiEnabled` 时渲染 `.mock-placeholder-banner` 横幅 + 每文件 `.mock-file-tag` 标签；样式复用项目既有 `--warning`/`--warning-bg` 令牌（[src/styles.css](../src/styles.css)）。
3. **"跳过校验"误判为"通过"** — 已修：[projectPatch.ts](../server/runtime/projectPatch.ts) `verifyPatchSetInSandbox` 无项目分支返回 `status:"open", skipped:true`（而非 `resolved`）；[workflow.ts](../server/workflow.ts) `runReview` 据此把 PatchSet 状态设为 `pending` 而非 `verified`。

> 3 项里第 1 项已被并行修复覆盖，第 2、3 项本轮完成并实测验证（curl 全链路 + 浏览器预览截图/无障碍树/`preview_inspect` 三重确认）。

---

## 三、模块地图

| 层 | 文件 | 职责 |
|----|------|------|
| 共享类型 | [src/types.ts](../src/types.ts) | Task/WorkItem/Assignment/GeneratedArtifact/PatchSet/ReviewFinding/ProjectWorkspace/AiProviderConfig 等 |
| 引擎接口 | [server/agents/engine.ts](../server/agents/engine.ts) | `AgentEngine` 统一接口 |
| 引擎实现 | claudeEngine / openaiEngine / mockEngine | Anthropic SDK / OpenAI 兼容 fetch / 离线兜底 |
| 引擎工厂 | [server/agents/index.ts](../server/agents/index.ts) | `createEngine(config)` 按 provider 选引擎；`engineStatus` |
| 编排 | [server/mockAgents.ts](../server/mockAgents.ts) | `splitWorkItems`/`assignWorkItems`/`scoreSubagent`（语义分派）/canvas |
| 工作流 | [server/workflow.ts](../server/workflow.ts) | 状态机：createTask→…→runGeneration→runReview→submit/apply/回流 |
| 真实校验 | [server/runtime/verify.ts](../server/runtime/verify.ts) + [workspace.ts](../server/runtime/workspace.ts) | 隔离沙箱 + TypeScript 编译器静态校验（P0） |
| 补丁/落盘 | [server/runtime/projectPatch.ts](../server/runtime/projectPatch.ts) | 产物→PatchSet、沙箱验证、应用到真实项目（hash 校验） |
| 项目扫描 | [server/projectWorkspace.ts](../server/projectWorkspace.ts) | 扫描真实项目根、包管理器、脚本、git 状态 |
| 校验/错误 | [server/validation.ts](../server/validation.ts) + [server/errors.ts](../server/errors.ts) | 输入校验、类型化 `AppError` |
| 持久化 | [server/db.ts](../server/db.ts) | `node:sqlite` |
| HTTP | [server/app.ts](../server/app.ts) | 路由 + AI 配置热切换 |
| 桌面 | [electron/](../electron) | 主进程内嵌 Express + 设置存储 + 预加载桥 |
| 前端 | [src/](../src) | React 工作台 + 设置页 |

---

## 四、Agent 角色与分工（吸收 Gas Town 思想）

Gas Town 的价值不在复制 CLI/术语，而在把多 Agent 编排当成**可追踪的运行系统**：明确角色、持久化工作单、容量调度、审查门禁、问题升级。当前 8 个 subagent（[mockAgents.ts](../server/mockAgents.ts)）：

| Agent | 职责 | 默认档 |
|-------|------|--------|
| 协调调度 | 接目标、推进 7 步、维护交接与分派 | quality |
| 产品需求 | 生成/更新需求文档 | economy |
| 任务计划 | 拆 work item、定义门禁与依赖 | quality |
| 前端实现 / 后端实现 | 生成可审查代码产物 | economy |
| 代码审查 | 质量门禁，输出审查问题 | quality |
| 问题修复 | 接回流，定位责任 work item 并修复 | quality |
| 监督巡检 | 监控容量/失败/卡住与升级 | economy |

**分派**：[scoreSubagent](../server/mockAgents.ts) 按"技能 ∩ work item（标题/描述/`targetFiles`）+ 文件领域 + 风险偏好 premium + 元角色不接实现单 + 并发负载"打分（已替换旧的正则匹配）。

---

## 五、已完成

- **桌面端 + 真实 AI**：Electron 封装；`AgentEngine` 抽象 + Mock/Claude/OpenAI 三实现；多 provider（含 baseUrl）端到端 + 设置页 + 本地持久化；`economy/quality`→`haiku-4-5/opus-4-8`（默认）可覆盖。
- **真实项目集成**：项目扫描、PatchSet、沙箱验证、应用到真实磁盘（hash 校验）、ApplyRecord。
- **P0 真实执行闭环（有条件生效，语义已诚实标注）**：绑定项目时，产物落盘隔离沙箱 → 真实 `tsc` 校验 → 编译错误=`error` finding → 回流带真实错误上下文重生成；未绑定项目时明确标注"未验证"而非"通过"。
- **M1 语义分派**：能力打分替换正则，本轮实测确认真实生效（5 个 work item 分给 3 个不同 agent）。
- **apply/submit 语义已拆分**：[workflow.ts](../server/workflow.ts) `submit`（生成提交记录）与 `apply`（写真实磁盘，要求 PatchSet 已验证）是两个独立方法，各自校验前置条件。
- **Mock 模式内容可辨识**：占位内容有醒目 UI 提示，不会被误认为真实生成失败或真实代码。
- **git 基线已建立**：仓库已提交并推送到 `github.com/003jia/Multi-Agent-Factory`。
- 工程健康：`typecheck`/`test`(41)/`build` 全绿。

---

## 六、路线图（按优先级）

### 核心命脉——把"多 Agent"做成真的（北极星，连续两轮未动，建议下一步真正开工）
- **M2 · 依赖产物交接**：`WorkItem.dependencies` 已填但**生成时未消费**。生成某 work item 时注入其依赖项已生成产物的摘要，实现 agent 间"读彼此产出"。
- **M3 · Agent 真用工具迭代**：当前每个 agent 仍是**单次 LLM 调用披前缀**（`grep tool_use` 全仓库零命中）。改成 tool-use 循环——agent 能 read 项目文件/依赖产物、write/edit、沙箱跑 verify，按编译错误自我迭代到通过。Anthropic 用 SDK tool runner，OpenAI 用 function calling，对外仍是统一 `AgentEngine`。
- **M4 · 协调者(Mayor)**：用 LLM 把 plan 拆成带依赖图的 work items + 初分派，替代模板拆分。

> M2/M3/M4 是唯一能让"多 Agent 协作"从产品命名变成产品事实的工作，已经被搁置了两轮迭代——期间的开发力气都进了前端视觉重做（熔炼镇改版）、工程健壮性（并发锁、类型化错误、多 provider）和本轮的诚实性修复（P0）。这些不是浪费，但不能无限期替代北极星。

### 前端（当前方向，非阻塞）
- 产品名与包名已切到 **熔炼镇 / forge-town**，视觉方向以 `design-reference/smelting-town/` 和 [当前开发基线-熔炼镇.md](当前开发基线-熔炼镇.md) 为准。
- 首页已从"大工作台全部展开"收敛为熔炼镇隐喻。
- 任务详情页保留阶段卡片和真实执行链路，但 `src/App.tsx` 已 1446+ 行，建议在下次改动前先拆分组件，避免继续膨胀成单文件维护黑洞。

---

## 七、不变量（不能被绕过）

- 后端状态机阻止越级：未确认计划不能进入分派。
- 审查存在开放 `error` 时**不能提交/落盘**。
- 回流必须记录责任 finding / work item / subagent / note。
- 写真实磁盘前必须通过沙箱校验且无被阻止的变更集（hash 校验保护）。
- 无 Key 时降级 Mock，且 UI 必须明确区分"真实 AI / Mock / 运行时"，不假装在干活；跳过的校验必须明确标注"未验证"，不能显示为"通过"。

## 八、验证命令

```bash
npm run typecheck   # 期望 exit 0
npm test            # 期望 41 passed
npm run build       # 期望 exit 0
# 真实 AI：设置页或 env 配 ANTHROPIC_API_KEY / OPENAI 兼容端点后跑完整流程
```
